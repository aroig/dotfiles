[Unit]
Description=emacs client %i

Requisite=desktop.target 
After=desktop.target

[Service]
Type=simple
SyslogIdentifier=emacsclient
Slice=apps.slice

ExecStart=/usr/bin/emacsclient -s /run/user/1000/emacs/server -c


