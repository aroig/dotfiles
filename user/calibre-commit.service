[Unit]
Description=commit calibre library

Requires=calibre-stop.service
After=calibre-stop.service

[Service]
Type=oneshot

SyslogIdentifier=calibre-commit
ExecStart=/home/abdo/.config/systemd/scripts/git-commit.sh "$AB2_CALIBRE_DIR"
