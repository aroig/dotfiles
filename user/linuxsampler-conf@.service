[Unit]
Description=linuxsampler config %i

Requires=jack.service
After=jack.service


Requires=linuxsampler.service
After=linuxsampler.service


[Service]
Type=oneshot

ExecStart=/usr/bin/bash -c "cat ${AB2_CONF_DIR}/audio/linuxsampler/%i.lscp | nc localhost 8888 || true"

Slice=audio.slice

