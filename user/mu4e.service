[Unit]
Description=mu4e mail client

Requisite=graphical.target
After=graphical.target

Requires=emacs.service
After=emacs.service

Requires=git-use@mail.service
After=git-use@mail.service

[Service]
Type=simple
Slice=apps.slice

ExecStart=/usr/bin/emacsclient -s "%t/emacs/server" -c --frame-parameters='((name . "mu4e"))' --eval "(spacemacs/custom-perspective-@mu4e)"
KillSignal=SIGINT

