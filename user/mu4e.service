[Unit]
Description=mu4e mail client

Requisite=wm.target
After=wm.target

PartOf=mail.target

[Service]
Type=simple
SyslogIdentifier=mu4e
Slice=dropdown.slice

ExecStart=/usr/bin/emacs -mail --title "emacs-mail"
KillSignal=SIGINT

