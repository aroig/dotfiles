[Unit]
Description=gpg-agent forwarding
Requires=gpg-agent.service

[Service]
Type=simple
Restart=always

SyslogIdentifier=gpg-agent-forward
ExecStart=/home/abdo/.config/systemd/scripts/gpg-agent.sh forward
SuccessExitStatus=0 143
