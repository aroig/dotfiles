#!/bin/sh
#
# Client to emacs server

case "$TERM" in
    screen*|linux)
        exec /usr/bin/emacsclient -s "$XDG_RUNTIME_DIR/emacs/server" -t "$@"
        ;;
    *)
        exec /usr/bin/emacsclient -s "$XDG_RUNTIME_DIR/emacs/server" -c "$@"
        ;;
esac
