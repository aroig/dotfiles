#!/bin/sh
#
# Client to emacs org

# don't run as root
if [ `id -u -n` = 'root' ]; then
    echo "Never run emacs as root!"
    exit 1
fi

exec /usr/bin/emacs emacsclient -s "$XDG_RUNTIME_DIR/emacs/orgmode" "$@"
