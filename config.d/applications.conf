# Applications:
#
# Start a terminal
bindsym $mod+$ctl+Return exec sdrun termite@.service

# Start application launcher
bindsym $mod+x exec dmenu_run -p "Run:"

# Start file manager
bindsym $mod+$ctl+f exec sdrun vifm@.service

# Start browser
bindsym $mod+$ctl+b exec sdrun chromium@.service
bindsym $mod+$alt+b exec sdrun firefox@.service

bindsym $mod+$ctl+e exec sdrun emacsclient@.service

bindsym $mod+$ctl+i exec sdrun calibre.service

bindsym $mod+$ctl+m exec sdrun musicplayer.service

bindsym $mod+$ctl+o exec sdrun org.service

bindsym $mod+$ctl+u exec sdrun mu4e.service

bindsym $mod+$ctl+g exec sdrun glances.service


# Floating rules
for_window [id="pinentry"] float enable
for_window [id="pavucontrol"] float enable
for_window [id="wpa_gui"] float enable

for_window [id="mpv"] floating enable
for_window [id="feh"] floating enable

for_window [id="plugin-container"] floating enable
for_window [id="exe"] floating enable
for_window [id="vmpk"] floating enable

for_window [id="qemu-system-x86_64"] floating enable
